[metadata]
name = hermes-mind-link
version = 0.3.0

[options]
package_dir =
    = plugin
packages = find:
python_requires = >=3.9
install_requires =
    pyyaml>=6.0

[options.packages.find]
where = plugin

[options.entry_points]
console_scripts =
    mind-link = mind_link.cli:main
